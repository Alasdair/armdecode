# Sail ARM decode extractor

A small script to extract decoding information from the sail-arm spec into a TOML file

## Usage

```
make
```
then run
```
sail -plugin plugin.cmxs -arm_decode <files>
```
